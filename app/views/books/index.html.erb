<h1>投稿一覧</h1>

<% @books.each do |book| %>

  <p>本のタイトル</p>
  <span><%= book.title %></span>

  <p>感想</p>
  <span><%= book.body %></span>

  <span>
    <%= link_to "Show", book_path(book.id) %>
  </span>

<% end %>